{
  "name": "staking-dapp",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ staking-dapp",
    "create-local": "graph create --node http://localhost:8020/ staking-dapp",
    "remove-local": "graph remove --node http://localhost:8020/ staking-dapp",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 staking-dapp",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": { "matchstick-as": "0.6.0" }
}
